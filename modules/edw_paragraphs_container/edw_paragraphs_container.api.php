<?php

/**
 * Allows modules to alter the configuration of an individual column in the layout form.
 *
 * @param array $columnForm
 *   The form array for a single column in the layout configuration.
 */
function hook_edw_paragraphs_container_column_alter(array &$columnForm) {
  // Modify the column-specific configuration form.
}
